THREATLOCKER_URL=https://portal.threatlocker.com/
HEADLESS=false
# Use a dedicated Chrome profile folder to avoid conflicts with an already-running Chrome
# Example: C:\Users\<you>\AppData\Local\Google\Chrome\User Data\ThreatLockerProfile
USER_DATA_DIR=
# Optional profile name within the user data dir (e.g. "Default" or "Profile 1")
PROFILE_DIR=
# Optional if Chrome is not on PATH
CHROME_BINARY=
# Optional if chromedriver is not on PATH
CHROMEDRIVER_PATH=
# Reinjection interval (seconds)
INJECT_INTERVAL_SEC=5
